<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose renders its own Material 3 styling; this just needs to be a valid
         no-action-bar host theme so the Activity can start. -->
    <style name="Theme.MultiBarcodeReader" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/black</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
</resources>
